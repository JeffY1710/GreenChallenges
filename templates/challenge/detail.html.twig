{% extends 'base.html.twig' %}

{% block body %}


    <h1>Challenge Details</h1>

                    {# Ajoutez d'autres informations ici #}
    {# Ajoutez d'autres détails du challenge ici #}

    <div class="pannel_challenge">

      {# {{ dump(defis.title) }}
      {{ dump(defis.category) }} #}


      <div style="border: 1px solid black; width: 90px; height:90px; padding: 10px; margin: 10px; background-color: red">
      <h2>{{ defis.title }}</h2>
      <h2>{{ defis.category }}</h2>


              {# Ajoutez d'autres informations ici #}
      </div>
    </div>


{% endblock %}
